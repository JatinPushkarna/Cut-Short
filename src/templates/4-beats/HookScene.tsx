// "4 Beats" reuses 5-beats' hook treatment exactly -- same still-hook
// behavior (dimmed background, glitch text entrance), only the beat that
// follows it changes. Re-export, not a copy.
export * from "../5-beats/HookScene";
