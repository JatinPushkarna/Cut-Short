// "4 Beats" reuses 5-beats' hook-to-video transition exactly -- same glitch
// flash, now transitioning straight into the video beat instead of into a
// bridge card first. Re-export, not a copy.
export * from "../5-beats/GlitchFlash";
